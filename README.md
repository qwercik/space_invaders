# Space Invaders
3D adaptation of Space Invaders game

## Build instructions
To build the game run the following commands:
```bash
$ mkdir build/
$ cd build/
$ cmake ..
$ make
```

To run the game:
```
./space_invaders
```

